// content.js
// Currently unused, but ready if you want to inject functionality into web pages later.
// For now, it just logs that the extension is active.

console.log("UnBiased content script loaded.");
